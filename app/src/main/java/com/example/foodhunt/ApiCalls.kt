package com.example.foodhunt

// getSellLocations (Places API)
// api params: current latitude, current longitude, rank by distance, sell location
// return: list of place results with ids

// getPlaceDetails (Places API)
// api params: place id
// return: place details

// getDirections (Directions API)
// api params: current latitude, current longitude, destination latitude, destination longitude
// return: list of directions steps

// getSupermarketLocations (Places API)
// api params: current latitude, current longitude, search radius, supermarket type
// return: list of place results with ids